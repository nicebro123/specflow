明白,直接给你开跑流程。注意:这些命令要在**有 GPU 的那台机器**上、在 `code_0602_opo` 仓库根目录里执行(`launch_tmux.sh` 会记录生成时的工作目录)。

## 第 0 步:装好环境(每台机器一次)

```bash
pip install -e ".[data]" && pip install cell-eval
```

## 第 1 步:先跑一个 5 分钟冒烟测试(强烈建议)

因为我改过 launcher / train.py,先用极小步数验证「训练→存 checkpoint→cell_eval」整条链路通,再开大批量,避免挂一晚上才发现问题:

```bash
python scripts/train.py --config configs/norman.yaml --output-dir outputs/_smoke \
  --set training.max_steps=200 \
  --set training.eval_every_steps=100 \
  --set inference.n_control_cells=32
```

看到它最后打印出 `evaluation` 的 JSON(含 pearson_delta 等)、且 `outputs/_smoke/` 下有 `best.pt`、`agg_results.csv` 就说明链路 OK。确认后删掉:`rm -rf outputs/_smoke`。

## 第 2 步:主结果 —— Norman additive 5 个 fold(4 卡)

按 `EXPERIMENT_ORDER.md` 的最高优先级:

```bash
# 1) dry-run, 先看生成的命令和 run_config(不会真训练)
python scripts/launch_experiments.py --spec configs/experiments/additive_folds_4gpu.yaml

# 2) 确认无误后, 启动 4 张卡的队列
bash outputs/20260603_additive_folds_0602/launch_tmux.sh
```

## 第 3 步:Control / Additive 基线(同 split,5 fold)

```bash
python scripts/launch_experiments.py --spec configs/experiments/norman_baselines_4gpu.yaml
bash outputs/20260603_norman_baselines_0602/launch_tmux.sh
```

## 监控进度

```bash
tmux ls                          # 看各 GPU 队列 session
tmux attach -t <session名>        # 进去看实时输出 (Ctrl-b d 退出)
# 或直接跟某个 run 的日志:
tail -f outputs/20260603_additive_folds_0602/00_full_additive_f0_s42_g0/train.log
```

## 汇总结果

全部跑完后:

```bash
python scripts/summarize_experiments.py --root outputs --output outputs/experiment_summary.csv
```

得到 5 个 fold 的 mean±std,即 SpecFlow 在 Norman additive 上的主表。

------

几点提醒:

- **断点续跑**:队列脚本会自动跳过已有 `agg_results.csv` 的目录;某个 run 中途失败(已修复)不会拖垮整队,直接重跑同一条 `bash run_gpuN.sh` 即可补齐缺失的。
- **跑消融**:主结果稳定后,`configs/experiments/ablation_4gpu.yaml` 同样的方式起。
- **临时改超参单跑**:不用写 spec,直接 `python scripts/train.py --config configs/norman.yaml --output-dir outputs/xxx --set flow.sigma=0.25`。

需要我帮你确认某个 `configs/experiments/*.yaml` 里的具体实验组合(比如 baselines spec 是否 5 个 fold 都齐),或者根据 `EXPERIMENT_ORDER.md` 再排一版 holdout / 消融的执行清单吗?